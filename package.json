{
  "name": "context-mixing",
  "description": "A minimal repo to demonstrate a bug involving minification in Parcel",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html -d dist",
    "prod": "yarn build && yarn serve -s dist"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.4",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "serve": "^11.3.2"
  }
}